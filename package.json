{
  "name": "wdegen",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ sarvesh/wdegen",
    "create-local": "graph create --node http://localhost:8020/ sarvesh/wdegen",
    "remove-local": "graph remove --node http://localhost:8020/ sarvesh/wdegen",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 sarvesh/wdegen",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.70.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
